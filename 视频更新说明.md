# 幻核动漫官网视频更新说明

以后新增或替换案例视频，只需要维护一个文件：`src/content/cases.js`。

## 推荐方式：使用云端视频地址

1. 将视频和封面上传到现有云存储或视频存储服务。
2. 复制视频地址和封面地址。
3. 在 `caseVideos` 数组中复制一份作品模板，填写标题、类型、简介、时长、画幅和两个地址。
4. 保存后重新构建并发布网站。

访客看到的是作品封面和“播放作品”按钮。点击后直接在案例页中播放，不会跳转到视频地址。

## 新增一个作品

在 `caseVideos` 数组中加入：

```js
{
  id: "作品一",
  title: "这里填写作品标题",
  category: "仿真人",
  summary: "这里填写作品简介",
  videoSrc: "这里粘贴视频地址",
  posterSrc: "这里粘贴封面地址",
  duration: "01:30",
  orientation: "竖屏",
},
```

每个作品的 `id` 必须不同。数组中的排列顺序，就是案例页上的展示顺序。

分类只使用：仿真人、2D动漫、3D动漫、游戏广告、商业广告、文旅影像。画幅只填“横屏”或“竖屏”。

## 替换已有视频

- 如果云端新视频可以保持原地址不变，只替换云端文件，网站配置不需要修改。
- 如果新视频地址发生变化，只修改对应作品的 `videoSrc`。
- 如果只更换封面，只修改对应作品的 `posterSrc`。

## 使用本地文件

也可以把视频放入 `public/videos`，把封面放入 `public/covers`：

```js
videoSrc: "/videos/作品一.mp4",
posterSrc: "/covers/作品一.jpg",
```

本地视频会被一起打包发布。上线前应先生成网页优化版，保留原片不覆盖，避免视频文件过大影响加载。

## 暂时隐藏作品

暂时不想展示某个作品时，把该作品对象从数组中剪切到文件底部的注释区域；需要恢复时再放回数组即可。
